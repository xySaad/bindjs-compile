ref count = 0;

const inc = () => count++;
const dec = () => count--;

export const Counter = () => (
  <div class="counter">
    <h1>{count}</h1>
    <button onClick={inc}>+</button>
    <button onClick={dec}>-</button>
  </div>
);
