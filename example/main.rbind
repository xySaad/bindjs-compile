ref count = 0;

const pow2 = ()=>{ 
  const result = count * count;
  return result
}
const calculate = (operation) => {
  return operation()
};

export const Counter = () => {
  return (
    <div class={count > 10 ? "counter big" : "count"}>
      <h1>{count}</h1>
      <div class="double" data-double={pow2()}></div>
      <button onClick={inc}>+</button>
      <button onClick={dec}>-</button>
    </div>
  );
};
a;
